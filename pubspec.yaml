name: flutter_libmwc
description: Dart bindings for the MWC wallet.
version: 0.1.0

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.0.2
  decimal: ^2.1.0
  mutex: ^3.0.0
  code_assets: ^1.2.1
  hooks: ^2.2.0
  native_toolchain_rust: ^1.0.6


dev_dependencies:
  flutter_test:
    sdk: flutter
#  flutter_lints: ^2.0.0
